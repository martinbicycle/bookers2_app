<div class="container">
  <h1>Editing Book</h1>
  <%= form_with model:@blog, local: true do |f| %>
  <p>Title<p>
  <%= f.text_field :title %>
  <p>Opinion</p>
  <%= f.text_area :body %>
  <div><%= f.submit 'Update Book' %></div>
  <% end %>
</div>